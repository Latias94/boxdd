[package]
name = "boxdd-package-consumer-wasm-compile-only"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
boxdd-sys = { version = "0", default-features = false }

[features]
double-precision = ["boxdd-sys/double-precision"]

[workspace]
