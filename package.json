{
  "name": "boxdd-browser-tests",
  "private": true,
  "version": "0.6.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test:browser": "playwright test --project=provider-smoke",
    "test:pages-browser": "playwright test --project=pages-smoke"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1"
  }
}
